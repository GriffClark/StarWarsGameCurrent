Create an interface for forcefield and torpedo instead of classes and repeating methods, but not sure how the syntax for that would work
Fix ship selection and all of its many bugs
Find a way to calculate distance between two points
Make sure that the current move system works (it is not very efficient and thus not a final system, but it should work for now)
Fix the game loop to make it easier to work
Fix the pre game loop to make it actually managable for the average person
Figure out a better way to manage shop. display which ships are avalable for each player in a way that doesn't hard code it
add clause to torpedoes will actually detonate
fix move system

Torpedoes are explained in the TIEBomber class

this is an edit